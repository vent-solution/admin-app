export interface SpaceModel {
  spaceId?: number;
  spaceNumber: string;
  capacity: number;
  price: number;
  spaceCategory: string;
  availability: string;
  dateCreated?: string;
  lastUpdated?: string;
}
